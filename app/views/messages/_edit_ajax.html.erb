
<div class="col-md-3"></div>
<div class="col-md-6">

  <%= form_for @message, :url => message_path  do |m| %>


  <%= render :partial => "edit", :locals => { :message => @message } %>

      <%= m.submit "取消", :class => 'btn btn-danger', :status => "draft", :data =>{:confirm =>'r u sure?'} %>
      <%= m.submit "確定更新", :class => 'btn btn-primary'%>
  <% end %>
</div>
